"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _express = _interopRequireDefault(require("express"));

var _expressWs = _interopRequireDefault(require("express-ws"));

var _constants = _interopRequireDefault(require("../lib/constants"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var _default = (db, emitter) => {
  const StreamingController = _express.default.Router();

  (0, _expressWs.default)(StreamingController);
  StreamingController.ws('/', ws => {
    const listener = ([attributes]) => {
      ws.send(JSON.stringify(attributes), error => {
        console.error(error);
      });
    };

    const ping = setInterval(() => {
      ws.send('ping');
    }, 15000);
    emitter.addListener(_constants.default.STREAM_CRAWL_EVENT, listener);
    ws.on('close', () => {
      clearTimeout(ping);
      emitter.removeListener(_constants.default.STREAM_CRAWL_EVENT, listener);
    });
  });
  return StreamingController;
};

exports.default = _default;
module.exports = exports.default;